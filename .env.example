# Port on which the server will listen
# Default: 3100
PORT=3100

# Database configuration
# Options: sqlite (default) or postgresql
DB_TYPE=sqlite

# SQLite configuration (when DB_TYPE=sqlite)
# Path to SQLite database file
DB_PATH=./data/telemetry.db

# PostgreSQL configuration (when DB_TYPE=postgresql)
# Connection string for PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/telemetry
# Enable SSL for PostgreSQL (true/false)
# DATABASE_SSL=false

# Electron desktop viewer configuration
# URL where the UI should be loaded from when packaging/running Electron.
# Defaults to http://localhost:3100/event-log if unset.
# TELEMETRY_UI_URL=https://ibm-salesforce-context-telemetry.onrender.com/event-log
