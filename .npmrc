# Optimitzacions per accelerar npm install a Render
# Desactiva l'audit durant l'instal·lació (tarda molt i no és necessari durant el build)
audit=false

# Desactiva la barra de progrés per reduir overhead
progress=false

# Usa cache de npm quan sigui possible
prefer-offline=false
